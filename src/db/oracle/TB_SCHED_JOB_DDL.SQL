CREATE TABLE TB_SCHED_JOB
(
	SCHED_JOB_ID			CHAR(32)		NOT NULL,
	JOB_NAME				VARCHAR2(100)	NOT NULL,
	JOB_DESC				VARCHAR2(200),
	JOB_CLASS				VARCHAR2(200)	NOT NULL,
	JOB_CRON				VARCHAR2(100)	NOT NULL,
	JOB_NEXT_RUN_TIME		TIMESTAMP,
	JOB_STATUS				CHAR(1),
	ON_SUCCESS				VARCHAR2(200),
	ON_SUCCESS_DELAY		INTEGER,
	ON_FAILED				VARCHAR2(200),
	ON_FAILED_DELAY			INTEGER,
	ON_SUCCESS_WITH_ERROR   VARCHAR2(200),
	ON_SUCCESS_WITH_ERROR_DELAY INTEGER,
	CONSTRAINT PK_TB_SCHED_JOB PRIMARY KEY (SCHED_JOB_ID)
	
);